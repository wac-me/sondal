import React from 'react';
import ReactDOM from 'react-dom/client';
import SondalApp from '../sondal.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SondalApp />
  </React.StrictMode>
);
